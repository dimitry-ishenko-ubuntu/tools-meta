# .tmux.conf

# general
set-option -g copy-command "xsel -i -b"
set-option -g escape-time 10
set-option -g focus-events on
set-option -g history-limit 1000000
set-option -g mode-keys vi
set-option -g mouse on
set-option -a terminal-features "*:RGB,*:usstyle"

# status
set-option -g status-position top
set-option -g status-bg black
set-option -g status-fg white
set-option -g status-left-length 0
set-option -g status-left "#[fg=black,bg=color136,bold] #S #[nobold]"
set-option -g window-status-format "#[fg=black,bg=color241]#[fg=white] #I:#W #[fg=black]"
set-option -g window-status-current-format "#[fg=black,bg=color75]#[bold] #I:#W #[nobold]"
set-option -g window-status-separator ""
set-option -g status-right "#[bg=color112,fg=black,bold]#{?pane_in_mode, SCROLL ,}"

# bind
bind-key -n M-0 select-window -t 0
bind-key -n M-1 select-window -t 1
bind-key -n M-2 select-window -t 2
bind-key -n M-3 select-window -t 3
bind-key -n M-4 select-window -t 4
bind-key -n M-5 select-window -t 5
bind-key -n M-6 select-window -t 6
bind-key -n M-7 select-window -t 7
bind-key -n M-8 select-window -t 8
bind-key -n M-9 select-window -t 9
bind-key -n M-c kill-pane
bind-key -n M-h select-pane -L
bind-key -n M-j select-pane -D
bind-key -n M-k select-pane -U
bind-key -n M-l select-pane -R
bind-key -n M-s split-window -v -c "#{pane_current_path}"
bind-key -n M-t new-window -c "#{pane_current_path}"
bind-key -n M-v split-window -h -c "#{pane_current_path}"
bind-key -n S-PageUp copy-mode -u
